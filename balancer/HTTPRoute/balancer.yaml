apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: balancer
  namespace: balancer
spec:
  parentRefs:
    - name: balancer
  rules:
    - backendRefs:
        - name: balancer
          port: 80
      matches:
        - path:
            type: PathPrefix
            value: /
