apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: vaultwarden
  namespace: vaultwarden
spec:
  hostnames:
    - vaultwarden.phl.io
  parentRefs:
    - name: vaultwarden
  rules:
    - backendRefs:
        - name: vaultwarden
          port: 80
