image:
  tag: 0.1.11

frontend:
  image:
    tag: 0.1.11

# Routing is handled by Envoy Gateway — see _gateways/third-places.yaml.
# Note the Gateway route splits /api/, /admin/, /static/admin/ to the Django
# backend and everything else to the frontend; this Ingress did the same.
ingress:
  enabled: false
