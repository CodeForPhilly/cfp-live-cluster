apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: grafana
spec:
  hostnames:
    - metrics.live.k8s.phl.io
  parentRefs:
    - name: grafana
  rules:
    - backendRefs:
        - name: grafana
          port: 80
