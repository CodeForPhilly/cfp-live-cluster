apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: balancer
  namespace: balancer
spec:
  hostnames:
    - balancerproject.org
  parentRefs:
    - name: balancer
  rules:
    - backendRefs:
        - name: balancer
          port: 8000
